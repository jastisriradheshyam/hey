version: 1
meta:
  type: module
  description: ""
tasks:
  ls:
    default:
      - type: spawn
        context:
          name: ls
          args:
            - -alh
    linux:
      - type: spawn
        context:
          name: ls
          args:
            - -alh
  test_env:
    default:
      - type: spawn
        context:
          env_vars:
            - key: 'hello'
              value: 'something'
          name: bash
          args:
            - "-c"
            - "echo $hello"
